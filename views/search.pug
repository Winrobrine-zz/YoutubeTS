extends layout
include mixins/video

block content
    .search__header
        h3 Searching for: #{keywords}
    .search__videos
        if videos.length === 0
            h5 No Videos Found
        each video in videos
            +video(video)