mixin video(video={})
    .video
        a(href=(routes.videos + routes.videoDetail(video.id)))
            video.video__thumbnail(src=video.src, width="263", height="148")
            h4.video__title=video.title
            if video.views === 1
                h6.video__views 1 view
            else
                h6.video__views #{video.views} views