mixin video(video={})
    .videoBlock
        a(href=(routes.videos + routes.videoDetail(video.id)))
            video.videoBlock__thumbnail(src=video.src, controls=true)
            h4.videoBlock__title=video.title
            h6.videoBlock__views=video.views